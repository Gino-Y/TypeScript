// 类型推断
let countInterence = 123;

const one = 1;
const tow = 2;
const three = one + tow;

const SuaiGe = {
    name:'Gino',
    age:18
}


// 类型注解
let count : number;
count = 123;

function getTotal(one : number, tow : number){
    return one + tow
}
const total = getTotal(1,2)

